# Python
__pycache__/
*.pyc
.venv/
venv/

# Secrets / credentials (절대 커밋 금지)
.wings_credentials
.totp_secret
*.secret

# Chrome profile (대용량, 로컬 전용)
.wings_chrome_profile/
wings_chrome_profile.zip

# Local WINGS downloads (Actions 가 생성)
wings_data/*.xlsx
wings_data/*.xls
wings_data/*.csv

# 원본 데이터는 SharePoint 에서만 관리한다 — repo 에 커밋 금지.
# 빌드가 SharePoint 에서 내려받아 아래 폴더를 채운다(로컬 파일은 유지되나 추적 안 함).
sam_files/
code/*.xlsx
code/*.xls
model_rules/*.xlsx
docs/rules.json
.sp_stage/

# Debug
_wings_*.log
_debug_*
